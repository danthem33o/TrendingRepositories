/// <reference types="cypress" />

describe("Trending repositories", () => {
  beforeEach(() => {
    cy.visit("/");
  });

  it("should show a list of trending repositories that have been created in the last 7 days", () => {
    //
  });

  it("should show the top 10 trending repositories", () => {
    //
  });

  it("should allow a user to see a full list of trending repositories", () => {
    //
  });
});
