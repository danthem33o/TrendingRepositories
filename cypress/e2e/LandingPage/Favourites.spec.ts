/// <reference types="cypress" />

describe("Favourites", () => {
  it("should show favourited repositories", () => {
    //
  });

  it("should show the most recent ten favourited repositories", () => {
    //
  });

  it("should allow a user to see a full list of favourited repositories", () => {
    //
  });
});
